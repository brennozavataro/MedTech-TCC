document.addEventListener('DOMContentLoaded',()=>{const search=document.getElementById('referenceSearch'),cards=[...document.querySelectorAll('.reference-card')],buttons=[...document.querySelectorAll('.filter')],empty=document.getElementById('emptyResults');let current='todos';function update(){const term=search.value.trim().toLocaleLowerCase('pt-BR');let count=0;for(const card of cards){const show=(current==='todos'||card.dataset.category===current)&&card.dataset.search.includes(term);card.hidden=!show;if(show)count++}empty.hidden=count>0}buttons.forEach(button=>button.addEventListener('click',()=>{current=button.dataset.filter;buttons.forEach(item=>{const active=item===button;item.classList.toggle('active',active);item.setAttribute('aria-pressed',String(active))});update()}));search.addEventListener('input',update)});
